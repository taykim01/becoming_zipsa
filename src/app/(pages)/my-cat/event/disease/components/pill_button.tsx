"use client"

import Button from "@/lib/button";

export default function PillButton() {
    
    return (
        <Button.UserAction onClick={()=>{}} iconType="Pill"  textColor="white">약 주기</Button.UserAction>
    )
}